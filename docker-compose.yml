version: '3'
services:
  web-server:
    image: meetingflow-webapp
    build: 
      app:
    build:
      context: .
      dockerfile: .Dockerfile
    ports:
      - '8080:8080'
    volumes:
      - ./:/usr/src/app
